server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # This line is what makes React Router / client-side routes work
    # on refresh (e.g. /dashboard) instead of showing an nginx 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
